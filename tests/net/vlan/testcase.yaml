common:
  depends_on: netif
  min_ram: 32
  tags:
    - net
    - vlan
tests:
  net.vlan.no.headroom:
    extra_configs:
      - CONFIG_NET_PKT_ALLOW_HEADROOM_ALLOCATION=n
  net.vlan.with.headroom:
    extra_configs:
      - CONFIG_NET_PKT_ALLOW_HEADROOM_ALLOCATION=y
